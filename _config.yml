# Date/Time
timezone:	Asia/Shanghai
highlighter: rouge

# Encoding
encoding: UTF-8

# Rendering drafts
show_drafts: false

# free-maker default values
# match by either path or type 'posts' 'pages'
defaults:
  -
    scope:
      path: ""
      type: "posts"
    values:
      layout: "post"